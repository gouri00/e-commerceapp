import 'package:flutter/material.dart';

class EdiScreen extends StatelessWidget {
  const EdiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Edit'),
      ),
    );
  }
}
